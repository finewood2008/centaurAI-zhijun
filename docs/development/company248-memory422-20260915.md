# 公司 248 本体检索 422 修复

## 计划与验收

现场 routing preview 返回 `RETRIEVAL_REQUEST_INVALID` / 422。本地及现场 worker 哈希一致：`memory_retrieval` 将完整元数据 JSON 当版本交给 `memory_index`，worker 原样发送，超过 Data Engine `retrieval.score` 的 256 字符限制；缺省版本空串亦不合法。自我概览绕过该评分路径，故部分问题正常。

1. 主代理修改 `backend/mindos/zhijun/memory_index.py`：复用本地摘要语义，将完整版本和完整文本生成 SHA256；截断仅用于传输文本，不丢失版本变化。
2. 测试代理仅新增 `backend/tests/test_memory_index_worker_contract.py`，覆盖普通本体检索、长/空版本、变化失效、多批次、错误不被吞；主代理运行原有本地索引与记忆回归。
3. 核对 192.168.0.7 的容器、worker 全部文件清单、容量修复及活动任务。备份后只更新 worker 所需文件和制品清单，不改变 Data Engine 校验、模型/授权或客户端安装包。
4. 隔离合成验证、服务健康与源文件哈希检查；保留用户数据库和回滚文件，不发送真实聊天。

依赖：SSH/Docker、目标源码挂载权限、现有 manifest 格式、无活动业务写入窗口。验收必须包含摘要对完整元数据/文本变化敏感、版本非空且64字符、旧容量补丁保持、原数据保留。健康/合成测试不等于真实客户端全流程已验收。

## 进度

- [x] 编码修复与回归。
- [x] 现场及制品预检。
- [x] 备份、切换和运行验收。

## 交付结果

2026-09-15 19:59:15 CST，定向 SSH 更新公司盒 Worker 的 `backend/mindos/zhijun/memory_index.py`，重启原容器；不是 OTA/Manager 升级，没有重建 Data Engine 或客户端安装包。

- 修复文件 SHA256：`1d45afafa79c050b78ad0cd48a09f27238806842375cc7d59a0cc8ba49b1475b`。
- 容器 ID 保持 `9ca51151539816deaa7410888f33f08a6ecc8f4b1b6764e725709f43cace4861`。
- 镜像保持 `sha256:436e7a2f44c381f2f61fe14f0a1c4ac107c8e0dff801976fbba1c8ed20b8e384`。
- 178 项源文件清单中只有上述文件变更；容器环境、挂载、只读根文件系统、启动和重启配置均保持。
- Data Engine store/manager 容量修复 SHA 与上一轮记录一致。健康接口 8618/8620 均 200，未授权工作区请求仍 401。
- 26 段会话、120 条消息、7 条本体及 7 条证据的原始数据库行逐项保留；876 条写入/未知类型防重回执保持。

私有备份：`/srv/centauros-releases/company248-memory422-backup-20260915`（目录0700、敏感文件0600）。含停机一致性状态/密钥备份、旧文件、原容器配置、升级前后完整 worker 哈希清单与 deployment.json。历史 SOURCES.json/QA manifest 保留原样；此次修订以该目录的 manifest-after.json 为准，不能将旧清单冒充当前制品。

若需回滚，先确认无活动业务写入，停止原容器，仅恢复备份中的 `memory_index.py` 至原源文件路径再启动；继续使用当前数据目录，不能拿旧数据备份覆盖后续写入。当前故障修复只有此一个生产文件，不应顺带回滚容量镜像。

## 测试证据与边界

- 新增严格 Worker 合同测试 11 项、16 subtests 通过：长/缺省/空版本、摘要稳定性、完整文本尾部及授权元数据变化、本地/Worker 摘要一致、多批次与 UTF-8 字节上限、真实 SQLite 本体普通检索、自我概览，以及错误仍显式拒绝。
- 原本地索引、记忆检索和上下文相关测试 68 项、3 subtests 通过；Worker、记忆授权/策略、程序员本体链路另外42项通过。
- 主代理将新增测试与上述8个原模块联合复跑：共121项、19 subtests通过。新增测试内存恢复旧wire行为后能重现原422，亦确认scope过滤不被摘要修复放宽。
- 额外抽查 `context_lookup_turn` 时，两个模型/配置切换测试因preview提前409 `ONLINE_SERVICE_CHANGED`失败；使用Git HEAD原`memory_index.py`在内存复测同样失败，确认并非本次改动引入。该既有基线问题未修改，不能宣称整个仓库全部测试通过。
- 在盒端当前镜像的无网络隔离容器中，加载待发布代码，并从实际 Data Engine `materials.py` 提取未改动的参数校验表达式：66 条合成记录分8批均合法；没有调用真实模型或写入用户业务。升级后又用容器内实际源文件重复通过。
- Data Engine 合同文件 SHA256：`91e3f1613426a260731b8095dbc18427e1ba7af8e63ed83090c21548a15979d9`。
- `git diff --check` 通过。已有 Starlette/AnyIO 弃用警告与本次修复无关。

上述不代表已发送用户真实提问进行端到端验收。请用户重新连接248，重试原先未发送成功的沟通提纲；不必再次创建或确认本体，也不必更换模型。现有客户端可直接使用本次盒端修复；上一轮前端轮询改动仍待后续安装包。
