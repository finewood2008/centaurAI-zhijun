# 在线理解与本地资料协作（2026-09-04）

## 使用方式

设置页分为「日常对话与理解」和「本地文件处理」。配置在线服务并不等于同意外发；用户仍需在新会话默认设置或当前对话的「启用在线理解」中明确确认。

旧会话首次改用在线时开启不携带旧历史的上下文，原记录不删除、不解除保护。也可保持「整段仅本地」。外部服务沿用当前配置，不新增模型、不自动降级；失败后在原消息选择重试或改用本地。

文件、画像及受保护历史按来源版本、服务和用途授权。预览可查看实际消息、系统提示及证据片段。未选中的必要来源仍会阻止请求；可改用本地或明确不使用这些资料。后台缺少授权时暂停，用户在对话/设置里查看并恢复。

## 阅读优先与默认授权更新（2026-09-04 下午）

- 对话正文不再与固定右栏争空间。判断草稿、本体与建档进度、观察与复盘放入全高抽屉的独立页签；桌面抽屉宽 600px，窄屏使用全宽。只有抽屉主体滚动，不再把草稿压缩在图谱下。关闭/切换页签保留未提交编辑，但刷新依然需要先保存。
- 模型与授权在页头只占一个紧凑入口，后台待授权任务只显示数量。设置说明、旧历史边界、错误和任务恢复都在「模型与授权」抽屉中，后台不自动弹窗。
- 新增**默认关闭**的「默认授权相关文字」。用户明确确认后，限定当前设备作用域与当前服务，覆盖对话、个人理解、草稿、判断、复盘及对应后台任务的实际所需文字，包括之后新增或修改的内容。启用时冻结已列明的用途集合，未来新增任务不自动继承。切换服务必须重新确认。
- 文件提取文字使用单独的 `includeFiles` 同意选项，默认不勾选。画像默认授权不能单独放行其文件来源；原文件不上传。整段仅本地、删除/撤回、跨设备、旧版本来源无法恢复及不明来源仍按原规则拦截。
- 这是一份持续有效的用户同意，不是批量生成永久资料授权。每次预览及实际 HTTP 前重新读取，关闭立即停止其后续效力；此前逐次批准的版本授权仍保留。「撤销本设备资料用途授权」同时关闭默认开关并清除逐次授权；单项撤销加入排除清单，重新开启默认开关也不自动解除。
- 增量表：`routing_auto_consent`、`routing_auto_exclusions`、`routing_auto_history`。修改使用修订号和事务，旧窗口不能覆盖撤销；来源审计与回复依据保存本轮实际使用的授权类型、设置修订。`PUT /api/mindos/conversations/{id}/routing/default-consent`（id 可为 default）不会自行启用在线模式或恢复旧后台任务。
- 合成验证：298 项后端测试及前端契约测试通过。浏览器确认草稿切换/关闭保留编辑，默认授权开启后相关追问不弹窗且仅一次模拟调用，关闭后相关派生内容重新被拦截；390px 窄屏无横向溢出，输入框可见、抽屉全高。真实资料没有被默认授权，测试使用独立临时数据库与录制型假服务，没有访问真实模型。
- 本次部署前分别备份 `ontology`、`conversations`、`growth` 至 `data/db/<名称>.pre-reading-ui-20260904-161834.db`，三个备份完整性检查均为 `ok`。备份时有 17 段会话、116 条消息，保留用户在开发期间新增的数据。

## 实现边界

- `zhijun/routing.py` 统一组装上下文与检查来源；`routing_routes.py` 提供预览、授权、撤销、审计、恢复接口。
- `RoutingStore` 在原本体数据库增量创建模式、授权、预览、审计与待处理表。没有把旧开关、已确认记录或历史置信度转换为外发授权。
- 日常输入不先交本地模型改写或分类。相关理解采用本地词面检索；贴合度只作相关候选的小幅排序依据。
- 近期历史继承来源限制。涉及受保护历史的相关追问进入授权预览；独立新话题可排除这些历史并显示提示。相关性采用保守词面/指代规则，并非完备的意图识别；缺少上下文时模型应询问。
- `GuardedProvider` 在调用及实际 HTTP 发送前重新验证服务、模式、版本和授权。传输层阻止未通过检查的旧在线问答入口，连接测试只允许固定无个人数据文本。在线请求不跟随重定向；禁用域名边界保留。
- 后台提取只读取用户原话及必要上一轮回复，不额外注入全库画像/实体。模型输出是候选，不自动确认或改写正式记录；派生候选通过证据 locator 保存来源。初次观察、草稿、复盘和整理同样检查实际来源。
- 文件在本机保存、解析、分块；回答使用原有正文快照与片段，文件原件不外发。画像授权不绕过文件授权。撤回、删除和设备作用域在本地处理时也有效。
- 来源失效或旧版本来源链无法恢复时保守阻止在线使用，不能通过重新点击授权洗掉限制；重新选择当前来源生成新内容，或使用不携带旧历史的上下文。
- 通用上下文包和 USER.md 不导出这些深层派生字段。旧摘要不加入新的在线上下文，在线对话直接使用经检查的近期消息。
- 模型失败保留消息；确定性请求/回复 ID 防止重试重复。交互请求优先于等待中的后台请求，但不抢断已运行的模型调用。

## 验证与回滚

上线前 SQLite 在线备份（均通过完整性检查）：

- `data/db/ontology.pre-dual-routing-20260904-135307.db`
- `data/db/conversations.pre-dual-routing-20260904-135307.db`
- `data/db/growth.pre-dual-routing-20260904-135307.db`

实现阶段回归：290 项后端测试、18 个子测试通过；前端契约测试和生产构建通过。浏览器串联检查覆盖显式在线启用、旧历史排除、实际载荷授权、刷新、在线失败后本地重试和移动端布局；判断草稿与情境复盘的端到端回归也通过。新增测试检查后台额外画像输入、候选不自动确认、派生来源撤销、实际 HTTP 前的撤销、旧入口绕过、校准反馈循环、本地生命周期，以及贴合度等级进入实际请求并在刷新后的回执中保留。

固定合成能力检查：`backend/scripts/dual_routing_smoke.py`。只读取当前服务配置，使用临时本体/会话/授权库，不读取用户聊天、画像或文件；必须显式传 `--allow-online-synthetic`。结果位于 `data/diagnostics/task-routing/quality-smoke.json`。

5 组案例分别由当前在线服务（配置模型名 `deepseek-v4-pro`）和本地 `qwen3.5:9b` 回答：模糊意图、多重约束、跨轮追问、文件片段与个人处境、愿望与行为。两端均完成，在线约 3–6 秒，本地约 19–27 秒。该小样本不证明普遍能力优越；回答仍可能把用户陈述误归为文件证据、过度概括或使用无效引用。来源校验与用户确认不能被模型流畅度替代。

回滚时先停止服务，另行备份现状，再由机主决定恢复哪个备份；不能覆盖上线后新消息。新增表为增量迁移，保留原数据格式。

## 本机运行依赖检查

恢复了原配置的 `BAAI/bge-small-zh-v1.5` 嵌入模型缓存（仅用于本地索引，不是第二个在线模型）。配置文件来自 Hugging Face；权重通过 ModelScope 的同名仓库下载，并核对 SHA-256 与 Hugging Face 发布一致：`354763b9b1357bc9c44f62c6be2276321081ed2567773608c0d0785b61d5a026`。禁网加载和合成文本编码返回 `(1, 512)`。本地 Ollama 服务也已恢复监听 `127.0.0.1:11434`。
