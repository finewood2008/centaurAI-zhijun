# 公司 248 容量回退恢复

## 事实与计划

用户确认修复。只读现场：192.168.0.7 的 zhijun-integration-0907 在 19:01（CST）重建，镜像 4096614407444882577986741e38d11fe4c5aacf3316013613280fecf4ac2843，未包含此前两个容量补丁挂载；运行 store.py 为旧 1024 历史记录限制及 3600 秒读取回收逻辑。当前 jobs=1024，工作区文件约 2MB，磁盘空闲 270GB。

1. 主代理核查活动任务和镜像/挂载，保留现有新 Data Engine 镜像其它更新，仅补齐 store.py/manager.py；采用含补丁的派生镜像而非临时源文件挂载。
2. 停机前确认无运行写入，停机后一致性备份状态/密钥（仅留盒端私有目录），保留旧容器/镜像回滚。不得清空 jobs 或删除写入防重回执。
3. 前端子代理修改文件状态轮询：容量错误停止自动重试，临时故障有界退避，保留手动恢复/历史附件/会话隔离并测试。
4. 发布子代理核查 Data Engine 升级制品门禁，防止缺少容量语义的旧制品通过发布检查；不发布其它团队无关改动。
5. 验收运行源码哈希、健康与未授权拒绝、结果槽与回执保留、既有数据不被覆盖；记录本地合成验证与真实用户端验收边界。

依赖：现有盒端 SSH、Docker、原配置与数据挂载；本地容量回归通过；切换期间有短暂连接中断。仅容量修复和客户端轮询范围，不更新 Admin/Remote Agent/模型，不主动发起真实聊天，不重新打客户端包。

## 进度

- [x] 根因核查与范围确认。
- [x] 本地回归、派生镜像准备和升级防回退检查。
- [x] 活动任务核查、备份与切换。
- [x] 健康、数据保留及运行版本验收。
- [x] 前端轮询修复与验证。

## 现场交付结果

2026-09-15 19:32:10 CST 切换成功。保留 19:01 镜像的其它 Data Engine 更新，仅将两个容量修复文件内置到派生镜像；没有更新 Admin、Remote Agent、模型或用户数据。

- 镜像：`zhijun-data-engine:company248-capacity-20260915-r2`
- 镜像 ID：`sha256:436e7a2f44c381f2f61fe14f0a1c4ac107c8e0dff801976fbba1c8ed20b8e384`
- 当前容器 ID：`9ca51151539816deaa7410888f33f08a6ecc8f4b1b6764e725709f43cace4861`
- store SHA256：`f053f0bdd6ef181a78fef014a2ad16079efa0ade278ab2ebddf7fb7e83574571`
- manager SHA256：`2d44b15ee4bf4149147e2d39bd32a545befceddb176ecf4ee0513adf34438391`
- 容器 healthy、只读根文件系统、原六项数据/信任/模型/worker 挂载与环境配置不变，重启策略 `unless-stopped`。
- 8618/8620 健康检查均 200，8620 未授权工作区访问仍 401。
- 857 条写入或未知类型防重记录的 ID、状态、指纹、创建/更新时间、所有权逐项不变；120 条既有聊天消息及原会话行全部保留。
- 切换后任务记录 1025、完整结果 268、过期回执 757，活动任务 0。历史任务可超过 1024，过期回执不再占完整结果槽；并未清空任务目录来规避限制。

首轮切换因 Docker 返回的挂载列表顺序不同触发严格校验，自动恢复原容器。确认所有挂载字段一致后，将校验改为按目标路径排序比较，重新备份、切换并通过。无数据回滚或清空。

### 备份和回滚边界

成功切换前私有备份目录：`/srv/centauros-releases/company248-capacity-recovery-backup-20260915-r3`（0700，文件 0600，含一致性状态/密钥备份、原容器配置及验收记录；仅保留盒端，不提交 Git）。首轮 r2 备份亦保留。

旧容器 `zhijun-integration-0907-before-capacity-recovery-r3` 已停止，重启策略为 no。若需回滚，仅在确认无活动写入后切换程序，继续使用当前数据目录，不用旧备份覆盖升级后新增的业务数据。旧版有已知容量缺陷，不能把回滚当作该问题的解决方案。

## 验证与后续发布

- 派生镜像在无网络、只读挂载生产状态的隔离容器中，将状态复制到临时内存文件系统，验证新读取任务可准入且 857 条防重记录不变；未执行真实模型或业务写入。
- 网关容量等回归 257 项通过；发布门禁/容器构建测试 50 项通过（主代理复跑 50 项）。
- 前端全量 `test:all` 228 项通过；`build:desktop` 含 TypeScript 检查通过，存在原有动态/静态混用导入警告，不影响构建。
- 两仓库 `git diff --check` 通过。

前端四个文件新增容量错误停止自动重试、临时故障最多 3 次自动重试、手动恢复，以及实际 transport/composable 回归。保留历史文件状态首次读取，不隐藏既有附件。本轮只完成源码和 desktop 构建验证，**未生成新安装包**；现有 0.1.27 不含该轮询修复，但可以直接受益于盒端容量修复。

Data Engine 的 `scripts/check_release_guard.py` 和测试/文档增加正式源码及实际 staging 的容量行为检查，后续制品必须由包含此门禁与容量实现的源码构建。门禁没有追溯修改其他已构建镜像，也不等于 OTA/Manager 所有发布渠道已自动升级。当前盒端派生镜像已内置补丁，不依赖临时 bind mount。

用户端真实重连、文件状态重试及后续聊天仍需用户操作确认；以上健康与隔离准入验证不冒充完整现场客户端端到端测试。
