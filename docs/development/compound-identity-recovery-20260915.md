# 复合身份候选修复与验收

## 现场结果

真实桌面连接公司 248：复合句“我不仅是一个程序员，还是一个大四的学生”整理完成，但 `candidates=1, suppressed=1, created=[]`；单独“我是一名大四学生”生成 working 候选并在本体可见。不能把普通回复或任务 done 当作记忆成功。

## 计划与范围

1. `extract.py`：明确并列身份的模型提示、精确原话下的省略主语识别、限定双身份预算。
2. `memory.py`：先进行同范围可见记录去重，再消耗准入预算；记录不含正文的过滤原因计数。
3. `test_memory_compound_identity.py`：独立测试代理负责回归；主代理负责实现、集成与真实验收。
4. 本地回归通过后，只更新公司 248 的相关知君 Worker 文件，保留备份；不更新 Data Engine 或其他团队文件。
5. 原句真实复测，检查原话证据、候选状态与前端展示。已有学生候选不能删除来制造成功；优先使用未写入正式记录的隔离测试账号/空间，或明确记录真实去重结果并结合隔离生产链路验证。

## 验收约束

- 程序员与学生是独立身份，旧身份不得吞掉新身份。
- 完整原话与精确后半句均可作证据，不接受模型改写的 quote。
- 假设、否定、引用、第三人称和临时角色不因本次放宽进入长期候选。
- 新条目只为 working，正式记录仍需用户确认；来源、授权、章程约束不放宽。
- 保留现有脏工作区、生产对话、已确认记录和已生成学生候选。

## 进度

- 已完成：现场双场景对照、路径分析。
- 已完成：抽取/准入修复、代码复核、首轮盒端更新与原句真实复测。
- 已完成：收尾取消语义修复、正式客户端打包、本体提取再次验收。
- 未通过全链路无错误验收：首次新客户端对话存在流读取失败后空错误气泡；第二次对照完整显示。此残留不能归为已修复。

## 真实复测发现的收尾问题

21:53 的 `conv_858e72afba4d` 确实新增学生 working 候选 `clm_18120af6aead`，证据逐字为“还是一个大四的学生”。但模型扩写为“我是大四学生，正处在毕业阶段”，没有与旧学生候选词法去重。新旧候选均保留，不自动删除、确认或覆盖。

聊天后端消息 complete、网关 succeeded；抽取写入后派生 project 任务登记因 `WORKSPACE_EXECUTION_REQUIRED` 失败，异常使主抽取也标为 failed。已增加窄范围处理：保留已完成抽取结果，失败子任务继续独立可见，返回不含正文的 followupFailures，不重跑抽取、不放宽登记权限。

同时查明客户端确定存在错误取消语义：SSE 收到 message_done/error 后 reader.cancel 传入桌面 adapter，adapter 在网关 end 尚未到达时发出 product.cancel；网关即使原聊天 succeeded 也会撤销后台授权。这条路径由旧测试明确断言，已修正为专用进程内 Symbol 完成标记：仅停止本地读取/轮询；真正的用户停止、换盒、注销和异常仍取消。不能仅凭这条代码路径断言它解释了现场所有空回复 UI 症状，必须继续复测。

因此追加客户端相关流收尾文件及正式安装包；此前“仅盒端、无需客户端更新”的范围不再适用于最终修复。

## 本地验证

- 复合身份旧 HEAD 回归红测能复现漏记；新实现通过完整/后半 quote、不同连接词、拒绝篡改 quote、假设/第三人称、误合并、异主体、跨设备和来源不可见等边界。
- 最新后端联合回归：226 passed、139 subtests；使用一次性数据库与合成模型，非真实模型验收。
- 长期候选仍 working；角色量词等价去重不改已有条目或来源。明确复合角色的模型附加分句被裁为原话角色，不持久化推测的毕业/求职事实。

## 公司盒端最终部署

2026-09-15 22:09:08 CST，原容器、镜像、配置、挂载均保持，只更新 Worker 源码：

- extract.py：`c729bfa71cbd6993361bdc08a28ca3bcd28a2bb7bcb84ef0f081161912b017ee`
- memory.py：`6a18b0883e3b7dd421487025269de2094eeb6d38a9772b00646897e6993d9ae4`
- jobs.py：`82635a715027b308e606b7c69741afb59d090c791c967725a6211cd5631fdff5`

备份目录：`/srv/centauros-releases/company248-compound-followup-v2-backup-20260915`，仅盒端可读私有备份。30 个会话、138 条消息、9 条 claim 及证据逐行保留。8618/8620 健康 200；未授权 context 仍 401。

前一次部署检查因 Docker 返回挂载数组顺序变化触发自动回滚；已只读确认挂载排序后相同、配置未变、三个文件回滚成功且服务 healthy。修正为按目标路径比较后重新部署通过。没有删除或回退业务数据。

## 0.1.28 正式客户端与最终真实验证

- macOS arm64 DMG/ZIP 已生成，production 验包与迁移路径启动 smoke 均 ok；Developer ID 签名，未公证（notarize=false），不宣称完整 Gatekeeper 分发验收。
- DMG SHA256：`a32e0536b1a1dcb66ef3e9e975c07eef1ffaa8b575ef019b0ebd20e7158eedf3`
- ZIP SHA256：`71478bbce1ebb55f7bf2bc83c6a347affef74c51b629a216da191cc656a18559`
- 全量前端测试 231 passed；打包链路另外通过 shell、desktop、Electron E2E、typecheck/build、签名验包及启动检查。产物被 .gitignore 忽略，没有提交到仓库。
- 使用新构建的 release/mac-arm64/知君.app，未覆盖 /Applications 中旧应用；连接公司 248，通过桌面真实发送原始复合句，未直接调用抽取替代用户路径。
- 22:14 `conv_b0c75382b252` / `ojob_4967689f3f9c`：done/ok，modelCandidates=2、candidates=2、filterReasons.existing=2，created=[]，无 followupFailures。
- 22:18 同会话第二轮 `ojob_a8109b302820`：相同 done/ok、两项正确去重；界面完整显示回答，无本轮错误。
- 两轮均未增加重复候选、未确认或删除既有记录。21:31 单独句及 21:53 复合句生成的两条学生 working 候选保留。不能把最终两轮去重当成“重新新增”证明；新增证据来自前述真实写入，最终轮证明正确双身份抽取与去重。

### 尚未解决的界面残留

22:14 首轮 UI 显示空错误，而对应 gateway `16b4cde67cfa49ee00c726f24ca0c02c` 为 succeeded，持久化 assistant complete/178 字；网关保存完整 meta/provenance/token/extraction/message_done complete。当前只读检查不能确定客户端哪次 poll 失败。

代码复核确认：收到 meta 后如果后续读取失败，ConversationPage catch 把正式 ID 的空回复设为 error；refreshAfterTurn 不重新读取正文，故后台成功不会自动修复展示。未发现新建路由替换流中对象的确定竞态。下一步应捕获 poll 的准确错误码，并增加当前会话/消息身份受限的只读结果核对测试，禁止自动重发 POST。此问题不影响上述两轮抽取任务完成，但阻止将整个对话体验宣称“全部通过”。
